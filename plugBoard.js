const disc = require("./discs")

const trocarLetra = function (number,letter1,letter2){
    let letra1 = disc[number].indexOf(letter1)
    let letra2 = disc[number].indexOf(letter2)
    let letra3 = letra1
}

console.log(disc[0].indexOf('Y'))